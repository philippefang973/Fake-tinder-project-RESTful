import test from 'ava'
import Vuexpress from '../lib/vuexpress'

test('vuexpress is wrapper of express app', t => {
  t.plan(1)
  const app = new Vuexpress
  t.truthy(app.app, 'express application instance')
})
