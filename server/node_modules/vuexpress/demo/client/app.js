import vuexpress from 'vuexpress'
import modules from './store'
import routes from './views'

const app = vuexpress()

app.set('dock', [
  {
    label: 'Home',
    path: '/home'
  }
])

app.store(modules)

app.router({
  routes: routes
})

app.start()
